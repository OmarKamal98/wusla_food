import 'package:flutter/material.dart';

class FilterCellWidget extends StatelessWidget {
  const FilterCellWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
